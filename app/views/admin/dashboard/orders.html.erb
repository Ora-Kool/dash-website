<%=content_for(:page_admin, "admin") %>
<%=provide(:title, "Dashboard") %>
<div class="row">
        <div class="col-sm-3 col-md-2 sidebar">
          <ul class="nav nav-sidebar">
            <li class="active"><a>Overview <span class="sr-only">(current)</span></a></li>
            <li><%=link_to 'Orders', admin_dashboard_orders_path%></li>
            <li><%=link_to 'Contacts Messages', admin_dashboard_contacts_path%></li>
            <li><%=link_to 'LOGOUT', logout_path, method: :delete%></li>
          </ul>
          <!-- <ul class="nav nav-sidebar">
            <li><a href="#">Nav item</a></li>
            <li><a href="#">Nav item again</a></li>
            <li><a href="#">One more nav</a></li>
            <li><a href="#">Another nav item</a></li>
            <li><a href="#">More navigation</a></li>
          </ul>
          <ul class="nav nav-sidebar">
            <li><a href="#">Nav item again</a></li>
            <li><a href="#">One more nav</a></li>
            <li><a href="#">Another nav item</a></li>
          </ul> -->
        </div>

        <div class="col-sm-9 main">
          <h1 class="page-header">Dashboard</h1>

          <h2 class="sub-header">Orders Made By Customers</h2>
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Name/Business</th>
                  <th>Email</th>
                  <th>Phone</th>
                  <th>Message</th>
                </tr>
              </thead>
              <tbody>
              <%if @orders.blank? %>
               <p>No orders at made</p>
              <%else%>
              <%@orders.each do |order| %>
                <tr>
                  <td>1,00<%=order.id %></td>
                  <td><%=order.name_or_business %></td>
                  <td><%=order.email %></td>
                  <td><%=order.phone%></td>
                  <td><a href="#" data-target="#myModel<%=order.id%>" data-toggle="modal" >Read</a></td>
                </tr>
                <!-- Modal -->
              <div id="myModel<%=order.id%>" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                <div class="modal-dialog" role="document">
                  <div class="modal-content">
                    <div class="modal-header">
                      <h5 class="modal-title text-center" id="exampleModalLabel"><%= order.name_or_business %></h5>
                      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                      </button>
                    </div>
                    <div class="modal-body">
                      <%=order.message %>
                    </div>
                    <div class="modal-footer">
                      <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                     
                    </div>
                  </div>
                </div>
              </div>
                <%end%>
                <%end%>
              </tbody>
            </table>
          </div>
        </div>
 </div>
    